# EVE Online Web 项目 - 项目概览

> 完整的项目架构、文件结构、核心概念介绍

## 📌 项目简介

这是一个基于 **RuoYi-FastAPI** 架构开发的 EVE Online 工具型 Web 产品，提供以下功能：

- **用户认证**：通过 EVE Online SSO（单点登录）
- **数据管理**：导入和管理 EVE Online 静态数据（SDE）
- **管理后台**：完整的 Web 管理界面
- **API 接口**：RESTful API 供前端和第三方使用

---

## 🏗️ 架构设计

### 系统架构图

```
┌─────────────────────────────────────────────────────┐
│                   用户浏览器                          │
└────────────────────┬────────────────────────────────┘
                     │ HTTP/HTTPS
┌────────────────────▼────────────────────────────────┐
│              Nginx (反向代理)                       │
│          容器: eve_frontend (port 12580)           │
│   - 转发 /docker-api/* → Backend (port 9099)      │
│   - 提供 Vue3 SPA 静态资源                         │
└────────────────────┬────────────────────────────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
┌───────▼──────┐ ┌──▼──────┐ ┌──▼──────────┐
│  FastAPI     │ │ Redis   │ │ PostgreSQL  │
│  Backend     │ │ Cache   │ │ Database    │
│              │ │         │ │             │
│ 容器:        │ │ 容器:   │ │ 容器:       │
│ eve_backend  │ │eve_redis│ │ eve_db      │
│ (9099)       │ │ (6379)  │ │ (5432)      │
└──────────────┘ └─────────┘ └─────────────┘
        │
┌───────▼──────────────┐
│  SDE Processor       │
│  (后台数据处理)      │
│  容器: sde-processor │
└──────────────────────┘
```

### 核心模块

| 模块 | 技术栈 | 容器名 | 端口 | 功能 |
|------|--------|--------|------|------|
| **前端** | Vue3 + Vite | `eve_frontend` | 12580 | Web 管理界面 + 静态资源 |
| **后端** | FastAPI + Python 3.11 | `eve_backend` | 9099 | API 接口、业务逻辑 |
| **数据库** | PostgreSQL 15 | `eve_db` | 5432 | 业务数据、EVE SDE |
| **缓存** | Redis 7 | `eve_redis` | 6379 | 会话、缓存、队列 |
| **数据处理** | Python | `sde-processor` | - | 异步数据导入任务 |

---

## 📁 项目文件结构

```
/opt/EVE-Project/
│
├─ 📋 Docker 编排文件
│  ├─ docker-compose.yml              ❌ 已弃用（保留备用）
│  ├─ docker-compose.server.yml       ✅ 生产环境部署
│  ├─ docker-compose.local.yml        ✅ 本地开发部署
│
├─ ⚙️  配置文件
│  ├─ .env.server                     ✅ 生产环境变量
│  ├─ .env.local                      ✅ 本地开发环境变量
│
├─ 📖 文档文件
│  ├─ 1-PROJECT_OVERVIEW.md           📍 本文件（项目概览）
│  ├─ 2-DOCKER_BUILD.md               📍 Docker 构建与部署
│  ├─ 3-CONFIG_ENV.md                 📍 环境配置管理
│  ├─ 4-DEV_GUIDE.md                  📍 开发指南
│  ├─ 5-TROUBLESHOOTING.md            📍 故障排除
│
├─ 📦 EVE-FastAPI/ (主应用)
│  ├─ README.md                       项目初始 README
│  ├─ CHANGELOG.md                    版本更新日志
│  │
│  ├─ ruoyi-fastapi-backend/          后端应用
│  │  ├─ app.py                       FastAPI 应用入口
│  │  ├─ server.py                    启动脚本
│  │  ├─ Dockerfile.pg                后端镜像构建
│  │  ├─ requirements-pg.txt           Python 依赖（PostgreSQL 版）
│  │  ├─ .env.dockerpg                ✅ Docker 容器内应用配置
│  │  │
│  │  ├─ config/
│  │  │  ├─ env.py                    环境变量加载器
│  │  │  ├─ database.py               数据库配置
│  │  │  ├─ get_db.py                 数据库连接工厂
│  │  │  ├─ get_redis.py              Redis 连接工厂
│  │  │  ├─ ssh_tunnel.py             SSH 隧道管理
│  │  │
│  │  ├─ module_admin/
│  │  │  ├─ controller/login_controller.py    登录接口（含 EVE SSO）
│  │  │  ├─ service/login_service.py         登录逻辑（含 token 交换）
│  │  │  └─ ...
│  │  │
│  │  ├─ module_eve/                  EVE Online 相关模块
│  │  │  ├─ api/                      EVE API 接口
│  │  │  └─ schemas/                  数据模型
│  │  │
│  │  └─ alembic/                     数据库迁移管理
│  │     └─ env.py                    迁移配置
│  │
│  ├─ ruoyi-fastapi-frontend/         前端应用
│  │  ├─ Dockerfile                   前端镜像构建
│  │  ├─ package.json                 Node.js 依赖
│  │  ├─ vite.config.js               Vite 构建配置
│  │  ├─ .env.docker                  ✅ Docker 编译配置
│  │  │
│  │  ├─ src/
│  │  │  ├─ permission.js             路由守卫（SSO token 拦截）
│  │  │  ├─ layout/components/Navbar.vue   导航栏（SSO 登录按钮）
│  │  │  └─ ...
│  │  │
│  │  └─ bin/
│  │     └─ nginx.dockerpg.conf       ✅ Nginx 代理配置
│  │
│  └─ ruoyi-fastapi-test/             测试环境
│     ├─ docker-compose.test.pg.yml   测试编排文件
│     └─ requirements.txt              测试依赖
│
├─ 📦 eve-sde-processor/              SDE 数据处理模块
│  ├─ Dockerfile                      处理器镜像构建
│  ├─ main.py                         处理器入口
│  ├─ requirements.txt                依赖
│  ├─ config/settings.py              配置
│  ├─ core/
│  │  ├─ downloader.py                数据下载器
│  │  └─ importer.py                  数据导入器
│  └─ scripts/
│     └─ init_views.sql               初始化 SQL 脚本
│
├─ 📊 数据持久化
│  └─ postgres_data/                  PostgreSQL 数据卷（Docker 创建）
│
└─ eve_online.dump                    EVE Online SDE 数据备份
```

---

## 🔑 核心概念

### 1. Docker Compose 编排

**根目录编排** (`docker-compose.yml` / `.server.yml` / `.local.yml`)
- 协调 5 个容器的启动顺序和依赖关系
- 定义网络、卷、环境变量
- 后端构建路径：`./EVE-FastAPI/EVE-fastapi-backend`
- 前端构建路径：`./EVE-FastAPI/EVE-fastapi-frontend`
- 处理器构建路径：`./eve-sde-processor`

**子目录编排** (`EVE-FastAPI/docker-compose.yml`)
- 相对路径配置，适合在子目录开发
- 非主要使用文件

### 2. 配置管理体系

**三层配置**：

```
Layer 1: Docker Compose 编排层
         ↓
         .env.server / .env.local
         (Docker 变量替换，容器环境注入)
         
Layer 2: 应用配置层
         ↓
         .env.dockerpg (后端)
         .env.docker (前端编译)
         (应用启动时读取)
         
Layer 3: 代码内置层
         ↓
         config/env.py (Pydantic 默认值)
```

**优先级**（从高到低）：
1. Docker 环境变量（来自 `docker-compose.yml` 的 `environment:` 部分）
2. `.env.dockerpg` / `.env.docker` 文件中的值
3. 代码中的默认值

### 3. 数据库连接

**容器内通过服务名进行 DNS 解析**：

```python
# ✅ 正确：使用服务名
DATABASE_URL = "postgresql://user:pass@eve-pg:5432/dbname"

# ❌ 错误：使用容器 ID
DATABASE_URL = "postgresql://user:pass@abc123def456:5432/dbname"  # 无法解析
```

**网络**：所有容器在 `eve-network` 桥接网络中

### 4. EVE SSO 流程

```
1. 前端点击 "Login with EVE SSO"
   ↓
2. 后端生成 state，存入 Redis，重定向到 CCP 授权页
   ↓
3. 用户授权后，CCP 重定向到后端回调接口
   ↓
4. 后端验证 state，用 code 交换 access_token
   ↓
5. 后端解析用户信息，落库或自动注册
   ↓
6. 后端生成系统 JWT，重定向回前端
   ↓
7. 前端保存 JWT，自动登录
```

**关键配置**（需要在三个地方保持一致）：

```bash
# 1. 后端环境变量
EVE_CLIENT_ID=0f2b035a0dfa4487afb4fbc80668fae4 (本地) / 0c7843... (线上)
EVE_CLIENT_SECRET=eat_2EIXRPiDGUn355E63y69MfoS7ZgZTse8_1e0QXy (本地) / eat_1B... (线上)
EVE_CALLBACK_URL=http://localhost:12580/docker-api/auth/eve/callback (本地) / http://43.163.228.205:12580/docker-api/auth/eve/callback (线上)

# 2. CCP 应用后台
# 必须与 EVE_CALLBACK_URL 完全一致

# 3. Nginx 代理路径
location /docker-api/ {
    proxy_pass http://eve-backend-pg:9099/;  # 保证路径可达
}
```

---

## 🌐 网络架构

### 容器网络

```
Network: eve-network (bridge)
├─ eve-pg          (DNS: eve-pg, 内部 5432)
├─ eve-redis       (DNS: eve-redis, 内部 6379)
├─ eve-backend-pg  (DNS: eve-backend-pg, 内部 9099)
├─ sde-processor   (DNS: sde-processor)
└─ eve_frontend    (DNS: eve_frontend, 内部 80)
```

### 端口映射

| 服务 | 容器内部 | 宿主机映射 | 说明 |
|------|---------|-----------|------|
| 前端 | 80 | 12580 | Nginx 反向代理 |
| 后端 | 9099 | 19099 | FastAPI API |
| 数据库 | 5432 | 5432 (生产) / 15432 (本地) | PostgreSQL |
| Redis | 6379 | 仅内部 | 缓存服务 |

---

## 📊 启动流程时间轴

```
T+0s    docker-compose up -d 执行
        │
T+1s    ├─ eve-pg 启动
        │  └─ PostgreSQL 初始化
        │
T+3s    ├─ 健康检查通过 ✅
        ├─ eve-redis 启动 ✅
        │
T+3s    ├─ eve-backend-pg 开始构建
        │  ├─ 拉取基础镜像
        │  ├─ 安装系统依赖
        │  └─ 安装 Python 包
        │
T+30s   ├─ eve-backend-pg 容器启动
        │  ├─ 加载 .env.dockerpg
        │  ├─ 初始化数据库连接池
        │  └─ 启动 FastAPI 服务
        │
T+30s   ├─ sde-processor 启动（可选）
        │
T+35s   ├─ eve_frontend 构建
        │  ├─ npm install
        │  ├─ npm run build:docker
        │  └─ Nginx 启动
        │
T+40s   ✅ 所有服务就绪
```

### 构建时间估算

| 阶段 | 首次构建 | 增量构建 |
|------|---------|---------|
| 基础镜像拉取 | 2-5min | < 10s |
| Python 依赖 | 3-5min | < 30s |
| Node.js 依赖 | 2-3min | < 30s |
| 前端编译 | 1-2min | 30-60s |
| **总计** | **8-15min** | **< 2min** |

---

## 🔗 相关文档

- [2-DOCKER_BUILD.md](2-DOCKER_BUILD.md) - Docker 构建与部署
- [3-CONFIG_ENV.md](3-CONFIG_ENV.md) - 环境配置管理
- [4-DEV_GUIDE.md](4-DEV_GUIDE.md) - 开发指南
- [5-TROUBLESHOOTING.md](5-TROUBLESHOOTING.md) - 故障排除

---

## ⚡ 快速开始

### 本地开发部署

```bash
# 1. 查看配置
cat .env.local

# 2. 启动所有服务
docker-compose -f docker-compose.local.yml --env-file .env.local up -d

# 3. 查看日志
docker-compose -f docker-compose.local.yml logs -f

# 4. 访问应用
# 前端: http://localhost:12580
# API: http://localhost:19099/docker-api/docs
# 数据库: localhost:15432
```

### 生产环境部署

```bash
# 1. 查看配置
cat .env.server

# 2. 启动所有服务
docker-compose -f docker-compose.server.yml --env-file .env.server up -d

# 3. 查看日志
docker-compose -f docker-compose.server.yml logs -f

# 4. 访问应用
# 前端: http://43.163.228.205:12580
# API: http://43.163.228.205:19099/docker-api/docs
```

---

## 📞 支持与帮助

- 常见问题：见 [5-TROUBLESHOOTING.md](5-TROUBLESHOOTING.md)
- 环境配置：见 [3-CONFIG_ENV.md](3-CONFIG_ENV.md)
- 开发指南：见 [4-DEV_GUIDE.md](4-DEV_GUIDE.md)
- Docker 详情：见 [2-DOCKER_BUILD.md](2-DOCKER_BUILD.md)
